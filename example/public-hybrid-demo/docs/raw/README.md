# Raw Sources

These notes are the fallback-only authoring surface for the compiled wiki.

- edit raw notes first
- rebuild the topic with `python3 scripts/wiki_sync.py refresh <topic>`
- run `python3 scripts/wiki_sync.py lint`
