{
  "version": "${VERSION}",
  "project": "${PROJECT_NAME}",
  "last_updated": "${DATE}",
  "current_phase": "${CURRENT_PHASE}",
  "next_phase": "${NEXT_PHASE}",
  "benchmark_snapshot": {
    "summary": "${YOUR_CURRENT_BEST_METRIC}",
    "date": "${DATE}"
  },
  "do_not_rerun": [],
  "notes": "This file tracks version and experiment control. Agents read 'version' and 'do_not_rerun' every session.",
  "wiki_sync": {
    "default_mode": "optional",
    "raw_dir": "docs/raw",
    "knowledge_dir": "docs/knowledge"
  }
}
